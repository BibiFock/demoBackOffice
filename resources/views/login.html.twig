{% extends 'layout.html.twig' %}
{% set active = 'login' %}

{% block content %}
	{% if error %}
        <div class="alert alert-error">
            {{ error }}
        </div>
    {% endif %}

    <form action="{{ path('manage_login_check') }}" method="post" {{ form_enctype(form) }} class="form-vertical">
        	{{ form_widget(form) }}
            <button type="submit" class="btn btn-primary">{{ 'Login' }}</button>
		</div>
    </form>
	</div>

{% endblock %}
