			<div class="span3">
				<div class="well sidebar-nav">
					{% if(is_granted('IS_AUTHENTICATED_FULLY') ) %}
					<ul class="nav nav-list">
						{% set userToken = app.security.token.getUser() %}
						{% set sections = userToken.getAdminSections() %}
						{% if sections|length > 0 %}
							<li class="nav-header">Admin Part</li>
							{% for section in sections %}
								<li class="{{ section.name == active ? 'active' : '' }}"><a href="{{ path('manage.' ~ section.name) }}">{{ section.name }}</a></li>
							{% endfor %}
						{% endif %}
						{% set sections = userToken.getUserSections() %}
						{% if sections|length > 0 %}
							<li class="nav-header">User Part</li>
							{% for section in sections %}
								<li class="{{ section.name == active ? 'active' : '' }}"><a href="{{ path('manage.other', {'page' : section.name}) }}">{{ section.name }}</a></li>
							{% endfor %}
						{% endif %}
<!--						
							{% if(is_granted('IS_AUTHENTICATED_FULLY') ) %}
							<li class="nav-header">Admin Part</li>
							{% set menus = ['rights', 'users', 'sections'] %}
							{% for menu in menus %}
							<li class="{{ menu == active ? 'active':  ''}}"><a href="{{ path('manage.' ~ menu) }}">{{ menu }}</a></li>
							{% endfor %}
						{% endif %}
						
						<li class="nav-header">Sections</li>
						<li ><a href="#">section 1</a></li>
						<li ><a href="#">section 2</a></li-->
					</ul>
					{% endif %}
				</div>
			</div>
